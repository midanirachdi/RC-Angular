import { HtmlremovehrPipe } from './htmlremovehr.pipe';

describe('HtmlremovehrPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlremovehrPipe();
    expect(pipe).toBeTruthy();
  });
});
