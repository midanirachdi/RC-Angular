import { HtmlSlicePipe } from './html-slice.pipe';

describe('HtmlSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
